# Repository instructions

- Keep `docs/progress.md` synchronized whenever a verifiable development part is completed.
- Run `pnpm lint` after UI changes and fix every ESLint and `@shadcn/lint` violation.
- Run the stage-appropriate TypeScript, Java, Prisma and build checks before marking work complete.
- Preserve the domain boundaries documented under `docs/`; record architecture changes in a new ADR.

